export default (state = defaultState) => {
    return state;
};

const defaultState = {
    style: {
        height: "300px",
        width: "150px",
        backgroundColor: "#89C3B1",
        borderRadius: "3px",
        display: "flex"
    }
};

